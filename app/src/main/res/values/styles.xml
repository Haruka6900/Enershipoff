<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme applied before Compose takes over. Dark nautical. -->
    <style name="Theme.EnerShip" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/nautical_background</item>
        <item name="android:statusBarColor">@color/nautical_surface</item>
        <item name="android:navigationBarColor">@color/nautical_surface</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
